#include <bits/stdc++.h>
using namespace std;

#define rep(i,n) for(int i = 0; i < (n); ++i)
using ll = long long;
using vvi = vector<vector<int>>;

int main(){
    string s;
    cin >> s;
    s.pop_back();
    s.push_back('4');
    cout << s << endl;
}